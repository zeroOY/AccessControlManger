spring:
  mvc:
    view:
      prefix: /
      suffix: .jsp
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/accesscontroimanager?characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
    username: root
    password: zero

mybatis:
  configuration:
    auto-mapping-behavior: full
  mapper-locations: classpath:mapper/*.xml